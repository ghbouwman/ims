
-- test
local convert = require("convert")

local setups = require("setups")
local setup = setups.ims

for k, v in pairs(setup)
        print(convert.to_gu(v))



-- create the setup





-- main loop
